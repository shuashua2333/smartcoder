# 🔧 错误修复说明

## 错误信息

```
AI 掉线了: Failed to execute 'fetch' on 'Window': Failed to read the 'headers' property from 'RequestInit': String contains non ISO-8859-1 code point.
```

## 问题原因

在使用 `fetch` API 时，如果 headers 中包含非 ISO-8859-1 字符（如中文字符），会导致这个错误。

## 解决方案

已更新 `index.html` 中的 `callAI` 函数：

1. **使用 Headers API**：改用 `new Headers()` 来构建请求头，更好地处理字符编码
2. **添加 API Key 验证**：检查 API Key 是否已正确配置
3. **改进错误处理**：提供更清晰的错误信息

## 修改内容

### 之前（有问题）：
```javascript
headers: {
    "Content-Type": "application/json",
    "Authorization": `Bearer ${DEEPSEEK_KEY}`
}
```

### 现在（已修复）：
```javascript
const headers = new Headers();
headers.append("Content-Type", "application/json; charset=UTF-8");
headers.append("Authorization", "Bearer " + apiKey);
```

## ✅ 检查清单

如果还有问题，请检查：

1. **API Key 是否配置正确**
   - 打开 `index.html`
   - 找到 `const DEEPSEEK_KEY = "sk-你的key";`
   - 替换为你的实际 API Key（只包含 ASCII 字符）

2. **API Key 格式**
   - 应该是 `sk-` 开头的字符串
   - 不应该包含空格、换行或特殊字符
   - 示例：`sk-abc123def456...`

3. **浏览器控制台**
   - 按 F12 打开开发者工具
   - 查看 Console 标签页
   - 检查是否有其他错误信息

## 🎯 现在应该可以正常工作了！

如果还有问题，请告诉我具体的错误信息。

